import React from 'react';
import PropTypes from 'prop-types';
 
const Card = ({children}) => {
  return (
    <div>{children}</div>
  )
}
 
Card.propTypes = {
 
};<div>CARD</div>
 
Card.defaultProps = {
 
};<div>CARD</div>
 
export default Card<div>CARD</div>
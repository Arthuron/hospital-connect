const initialState = [{ name: "Krankenhaus 1" }, { name: "Krankenhaus 2" }, { name: "Krankenhaus 3" }];

export default function(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
